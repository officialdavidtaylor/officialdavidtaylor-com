# MinIO root credentials (store securely)
MINIO_ROOT_USER=replace-me
MINIO_ROOT_PASSWORD=replace-me-with-a-strong-password

# Optional only if using the embedded cloudflared service in docker/minio/docker-compose.production.yml
# CF_TUNNEL_TOKEN_MEDIA=replace-me
